/** Dimensão dos embeddings (voyage-code-3 com output_dimension 1024).
 *  Trocar de modelo exige migration + reindexação total — por isso vive num único lugar. */
export const EMBEDDING_DIMENSIONS = 1024;
